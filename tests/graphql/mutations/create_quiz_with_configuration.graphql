mutation CreateQuizWithConfiguration(
  $tenantId: ID!
  $tenantUserId: ID!
  $configuration: QuizConfigurationInput!
  $questions: [QuizQuestionInput!]!
) {
  quiz {
    create_quiz(input: {
      tenant_id: $tenantId,
      tenant_user_id: $tenantUserId,
      quiz: { configuration: $configuration, questions: $questions }
    }) {
      __typename
      ... on CreateQuizSuccess { correlation_id payload { id code quiz_type } }
      ... on IntegrityErrorResponse { message }
      ... on ValidationErrorResponse { message }
      ... on InternalErrorResponse { message }
    }
  }
}
